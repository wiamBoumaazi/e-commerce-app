//	in project folder
npm init

//	now install express
npm install --save express

//	now install nodemon
//	automatic reloading
npm install --save nodemon

//	now install morgan
//	default logging
npm install --save morgan

//	parsing the body and handling CORS
//	extract request body of an incoming request, like a post reqest
//	does not support files, but supports url-encoded bodies and json data
npm install --save body-parser

//	database
npm install --save mongoose